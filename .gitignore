node_modules/
dist/
*.local
.DS_Store
.vite/
*.tsbuildinfo

# Generated by `npm run data`. Rebuilt on every build, so it is not committed —
# the three source CSVs in scripts/ are the actual inputs.
public/data/
